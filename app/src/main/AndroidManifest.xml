<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meadberryfarms.foodapp"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".AdminProductDelete" />
        <activity android:name=".AdminChooseActivity" />

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

        <activity android:name=".AdminTrackOrdersActivivty" />
        <activity android:name=".UserDeliveryHistoryActivity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".AdminProductAddActivity" />
        <activity android:name=".AdminProductTypeActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".SelectLanguageActivity" />
        <activity android:name=".OrderPlacedActivity" />
        <activity android:name=".PlaceOrderActivity" />
        <activity android:name=".PaymentModeActivity" />
        <activity android:name=".PersonInformationActivity" />
        <activity android:name=".TotalItemsActivity" />
        <activity android:name=".ProductDescriptionActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewPasswordActivity" />
        <activity android:name=".EnterOTPActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
    </application>

</manifest>